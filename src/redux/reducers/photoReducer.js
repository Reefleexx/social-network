const initialState = {
    id: '',
    comments: [],
    comments_count: 0,
    likes: 0,
    url: ''
}

export default function photoReducer (state = initialState, action) {
    switch (action.type) {
        default: return state
    }
}